    // mapboxgl.accessToken=mapToken; 
    //mapboxgl.accessToken = process.env.MAPBOX_TOKEN || "FAKE_TOKEN";

   // const map = new mapboxgl.Map({
     // container: document.querySelector('.map'), // target the div with class 'map'
      //style: 'mapbox://styles/mapbox/streets-v12',
      //center:listing.geometry.coordinates, // longitude, latitude (example: New York)
      //zoom: 9
    //});

    //console.log(coordinates);
    // const marker = new mapboxgl.Marker(color:'red')
    //   .setLngLat(listing.geometry.coordinates)
    //   .setPopup(
    //     new mapboxgl.Popup({ offset: 25 }) // add popup
    //       .setHTML(`<h3>${listing.location}</h3><p>${listing.description}</p>`)
    //   )
    //   .addTo(map);
